title: Multikernel Technologies
email: contact@multikernel.io
description: >-
  Split-kernel architecture for Linux: run independent kernels on one machine with bare-metal performance and kernel-level isolation, no hypervisor.
baseurl: ""
url: "https://multikernel.io" # Set to your custom domain

# Build settings
markdown: kramdown
encoding: utf-8
permalink: /:year/:month/:day/:title/
plugins:
  - jekyll-feed
  - jekyll-sitemap

# Custom domain
github_pages:
  cname: multikernel.io

# Exclude unnecessary files
exclude:
  - README.md
  - Gemfile
  - Gemfile.lock
  - vendor/
  - .force-deploy
